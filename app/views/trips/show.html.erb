
Trip: <H3><%= @trip.name %></H3>
Start date: <%= @trip.start_date.strftime("%d/%m/%Y") %>
End date: <%= @trip.end_date.strftime("%d/%m/%Y") %>

<% @trip.trails.each do |trail| %>
  <%= trail.name %>
  <%= trail.length %>
  <%= trail.address %>
<% end %>